[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  # Permitir ambas keys en el scanner (GEMINI_API_KEY se usa en cliente, GITHUB_TOKEN solo en función)
  SECRETS_SCAN_OMIT_KEYS = "GEMINI_API_KEY,GITHUB_TOKEN"
